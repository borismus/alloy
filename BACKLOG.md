# Backlog

Tasks for the autonomous backlog runner (`scripts/backlog-runner.sh`).
Each `- [ ]` is one self-contained task. The runner picks the first open one,
works it on an `auto/<slug>` branch, verifies, and opens a PR for review.

Keep tasks small and unambiguous — one PR's worth of work each.

- [x] Add a unit test file src/utils/ids.test.ts for generateMessageId in src/utils/ids.ts, asserting the returned id matches the pattern msg-<4 hex chars>
- [x] Fix: in web mode (no Tauri, run via `npm run dev`), newly-created conversations don't appear in the sidebar until a manual reload, whereas Tauri mode updates live. In Tauri the vault watcher uses native `fs.watch`; in web it relies on the WebSocket file watcher in src/services/api/tauri-fs-http.ts (`/api/watch`) feeding useVaultWatcher, which drives App.tsx's `handleConversationAdded`/`handleConversationModified`. Investigate whether the server's watch endpoint emits `conversations/*.yaml` create events in standalone server mode (alloy-server notify/watch route) and whether the client watcher path-matching dispatches them. Expected: a new conversation file appearing in the vault while web mode is running shows up in the sidebar without a refresh, matching Tauri behavior.
- [x] Investigate high memory usage: the Alloy app was observed using ~8GB RAM at one point. It's a spike/leak, not steady state (normal is ~200MB WebKit WebContent + ~120MB native `Alloy` backend). Already ruled out on a first pass: RiffView's 1s `setInterval(loadDraft)` (has clearInterval cleanup), the WS file watcher in src/services/api/tauri-fs-http.ts (single socket, guarded reconnect), and the SSE `replay` handler in src/services/server-streaming.ts (dedups via `displayedLength`, so reconnects don't re-append). Leads to check: (1) mermaid/cytoscape diagram rendering during streaming — MarkdownContent re-renders on every chunk and mermaid is known to leak DOM/SVG on repeated renders; verify MermaidDiagram.tsx / DiagramBlock.tsx debounce and clean up. (2) base64 image data held in React state (vault loadImageAsBase64 / attachments). (3) the native `Alloy` backend (alloy-server) — tool file reads / large buffers in tool_loop and tools/. (4) src/utils/lineDiff.ts allocates an O(m·n) number[][] LCS table with no size cap — add a guard that short-circuits (skip collapse / fall back to a whole-block replace diff) when line counts exceed a threshold. Deliverable: identify the dominant contributor and add a bounded fix or guard for it.
- [x] Canonical config.yaml schema (v1) — DONE (schema portion). camelCase throughout; all providers under `providers:` with kinds openai_compatible / cli_claude; per-provider `local` flag (mlx-only privacy: padlock, private-dir access, offline tolerance; CLI kinds always cloud; public-URL safety rail); `version: 1`. Rust `config.rs` fails loudly on the old pre-0.4 format (no migration). SPA types, default template, first-run writer, and onboarding updated to v1 (native-provider cards dropped — all cloud via OpenRouter). Docs updated. 142 Rust + 126 TS tests pass. REMAINING (split to the follow-up below): making Rust the single parser via `/api/config` so the SPA stops using js-yaml.
- [x] Single-parser consolidation — DONE. Rust `config.rs` is the only config parser. Added `GET /api/config` (resolved config, provider API keys omitted; `sonioxApiKey` included for client-side dictation) and `PUT /api/config/favorites` + `PUT /api/config/value` (comment-preserving line splices ported from the SPA, with Rust tests). `src/services/vault.ts` now fetches `/api/config` and posts edits — removed the config `yaml.load`/`yaml.dump`, the dead `saveConfig`, and the splice helpers. First-run still writes the initial raw template (bootstrap, not a parser). 146 Rust + 116 TS tests pass.
- [x] Config v2 — unify subscription providers as `{ id, kind: cli, adapter: claude | codex }`, preserving provider/model IDs and each adapter's tested runtime behavior. Old `cli_claude` / `cli_codex` kinds and version 1 fail loudly with migration guidance. Local privacy is now explicit: only `local: true` on an OpenAI-compatible private-network endpoint grants the Local badge/private-directory access; omission is cloud, and CLI adapters reject `local` because they always send prompts to cloud services. Rust remains the single parser; templates, fixtures, SPA types, docs, and the active vault were migrated with a backup.
<!-- Design notes for the (now-landed) canonical config schema, kept for context:
  SCHEMA (v1): top-level `version: 1`, `defaultModel`, `favoriteModels`, `models`, `providers:` (list), `settings` (`externalEditor`, `shareOnNetwork`, `sharePort`), `services` (`email: {provider, apiKey, from, to}`), `privateReadOnlyDirs`, `compaction {enabled, triggerTokens}`, `serperApiKey`, `sonioxApiKey`. Everything camelCase.
  PROVIDERS: each entry `{ id, kind, baseUrl?, apiKey?, command?, oauthToken?, local? }`. Kinds: `openai_compatible` (OpenRouter cloud, oMLX local), `cli_claude`, and `cli_codex` (recognize the kind now; the impl is a separate task). Drop ALL legacy flat keys (`OPENROUTER_API_KEY`, `ANTHROPIC/OPENAI/GEMINI/XAI_API_KEY`, `CLAUDE_SUBSCRIPTION`, `CLAUDE_CODE_*`) and the `default_model`/`defaultModel` + snake/camel dual aliases from both Rust and TS.
  PRIVACY (`local`): `local: true` is privacy/offline and MLX-ONLY — it drives the padlock badge, eligibility to read `privateReadOnlyDirs`, and offline-tolerance (unreachable = temporarily offline, not a config error). `claude-cli` (and future `codex-cli`) are LOCAL PROCESSES but CLOUD DATA (prompts go to Anthropic/OpenAI) — never `local`, never private-dir access. When `local` is omitted, default from the endpoint (loopback → true); an explicit flag overrides; as a safety rail, gate private-dir reads on the endpoint not being a public host and warn if `local: true` is set on a public URL.
  SINGLE PARSER: Rust `config.rs` is the only parser. Remove js-yaml parsing/writing from the SPA (`vault.ts` getConfig/saveConfig/favorites, VaultSetup). Expose resolved config via a `/api/config` GET and route edits (favorites, share toggle, external editor) through endpoints; keep atomic writes; never log secrets.
  NO MIGRATION: if a loaded config still has old-shape keys, fail loudly with a clear onboarding/Settings message pointing at the v1 shape — don't silently start with zero providers.
  ALSO UPDATE: first-run VaultSetup writes v1; Settings; TS `Config`/`ProviderConfig` types (drop legacy key fields, add `local`, add `cli_codex` to the kind union); Rust + TS tests; docs (README, SETUP, CLAUDE.md). Preserve behavior: model IDs/defaults/favorites, MLX locality/privacy, Claude subscription auth, scheduled tasks, sharing, services (email), compaction, private dirs. Preserve Tauri and server modes.
-->
- [x] Add a `codex-cli` provider (kind `cli_codex`) to alloy-server, sibling to `cli_claude` (done). Shells out to `codex exec` in a read-only sandbox, prompt on stdin, `--output-last-message` for the clean answer + `--json` for token usage; scrubs `OPENAI_API_KEY` to force ChatGPT/Codex subscription billing; text-only (`supports_tools` false, no MCP bridge); always cloud (never `local`, no private-dir access). Config example + docs, and unit tests built from real codex-cli 0.145.0 output (prompt flattening, agent-message + usage parsing, unsupported-model error surfacing — 8 tests). Also aligned the `/api/models` "local" badge with `provider_is_local`. CURRENT: interactive turns no longer use the original one-shot exec path; app-server parity shipped below. `codex exec` remains only for internal one-shot title/compaction calls.
  - **Validated against codex-cli 0.145.0 (ChatGPT auth):** prompt-on-stdin works; `item.completed`/`agent_message`/`text` shape confirmed; `turn.completed.usage` gives token counts; obsolete/unavailable names such as `gpt-5-codex` are rejected with a 400. Follow-up discovery now reads the authenticated app-server `model/list` catalog and passes exact listed ids via `--model`, while retaining an explicitly labeled `codex-cli/default` route. Still needs a live end-to-end response through the app UI.
- [x] Vault watcher never catches up after a dropped connection, so the UI silently goes stale ("sometimes gets stuck not updating results"). Reproduced deterministically: with the backend bounced mid-session, a conversation created while disconnected NEVER appears (5 items before, still 5 after reconnect), while changes made after reconnect work fine — which is why it presents as intermittent. `src/services/api/tauri-fs-http.ts:218` reconnects the socket after 3s and re-registers watchers but never re-reads the vault, so every gap drops events permanently until a manual reload. Mobile hits this constantly (screen lock / app switch kills the socket). Fix: add an `onResync` callback to `useVaultWatcher`, fire it on reconnect AND on debounced `visibilitychange`, and have App re-read summaries/tasks/notes (~320ms measured on a 2216-conversation vault). TRAP: `loadConversationSummaries()` returns `messages: []`, so a wholesale `setConversations` blanks the conversation currently open — and the lazy-load effect is keyed on `conversations.length`, so it won't re-fire to fix it. The resync must MERGE, preserving `messages`/`messagesLoaded` for already-hydrated conversations and only invalidating ones whose `updated` changed. Two supporting bugs in the same file: (1) line 162, if a reconnect fails while another caller waits, the 100ms `setInterval` never clears and its promise never settles, so `watch()` hangs forever and the watcher never registers; (2) iOS leaves half-open sockets reporting `OPEN` with no data flowing, so `onclose` never fires and no reconnect is attempted — resyncing on foreground sidesteps this without needing a heartbeat. DONE: added `onWatchReconnect` to the fs shim (fired only on a re-open, not first connect) and an `onResync` callback on `useVaultWatcher`, triggered by reconnect AND debounced foreground (visibilitychange/focus) so half-open iOS sockets that never fire `onclose` are still covered. App re-reads summaries/tasks/notes and merges via `mergeConversationSummaries`, preserving hydrated messages and re-hydrating the open conversation when its `updated` changed. Also fixed the `ensureWebSocket` hang: queued callers are now settled on open/error/close instead of polling a never-cleared interval. Verified: the reproduction harness goes 5->6 (was 5->5 permanently stale), and the new smoke test fails in both projects when the resync is disabled. CORRECTION to this item's original claim: a naive merge does NOT permanently blank the open conversation — the lazy-load effect rescues it within the same tick (observed: renderedMessages 0 msgs then 2 msgs). The merge still avoids that flash and a redundant refetch. ACTUAL FOCUS/SCROLL ROOT CAUSE (the earlier identity/legacy-mtime theories were tested on-device and REVERTED): real vault ISO timestamps are unquoted YAML scalars, and js-yaml's default schema parses them into a fresh `Date` object on every read. The resync compared `existing.updated !== summary.updated`, so equal times from the summary and full conversation were ALWAYS unequal by object identity. Every focus therefore dropped the open conversation to `messages: []`, reloaded it, and caused auto-scroll to pin to the bottom. Conversation YAML now uses `yaml.JSON_SCHEMA`, preserving timestamps as strings at all three load boundaries. Regression coverage uses a tall real YAML fixture: scroll midway, dispatch focus, assert no loading state and exact scroll preservation. Critically, this test was run against the old parser and FAILED in both desktop and mobile (`sawReload: true`), then passed with the fix.
- [x] Model picker popover is stranded behind the iOS software keyboard (confirmed on-device against deployed alloy.z3.ca; NOT the v0.4.4 empty-catalog bug — the trigger renders correctly with its provider tag, so the catalog is fine). Symptom: opening the picker on mobile shows only the "Search models..." field just below the composer; the entire model list sits under the keyboard and is unreachable. Cause is a RACE between two listeners on the same `visualViewport` `resize` event: React Aria's `useOverlayPosition` (node_modules/react-aria/dist/private/overlays/useOverlayPosition.mjs:157, explicitly there to handle the iOS keyboard) calls `updatePosition()` measuring the trigger at its PRE-reflow position, while Alloy's `useVisualViewport` (`src/hooks/useVisualViewport.ts`) sets `--app-height`, reflowing `html, body, #root` (`src/App.css:31-40`) and moving the composer up by a few hundred px. Nothing repositions afterwards, so the popover keeps coordinates computed against the pre-keyboard layout. `autoFocus` on the SearchField (`ModelSelector.tsx:126`) opens the keyboard on EVERY mobile picker open, so the race fires every time; it is worst on a new conversation because the composer sits in a tall empty pane and therefore shifts furthest. NOT reproducible in Playwright (Chromium or WebKit): `setViewportSize` changes `window.innerHeight` and fires resize synchronously, so no race occurs — four attempts passed. Preferred fix: render the picker as a mobile bottom sheet (the `ui/Dialog` primitive already has a `max-width: 600px` bottom-sheet mode) so there is no positioning math to race. Minimal alternative: drop `autoFocus` on touch/narrow viewports so the keyboard does not open on picker open (also better UX — favorites are usually picked without typing). Avoid "force a second reposition after reflow" — that just re-orders the race. PARTIAL FIX SHIPPED: `autoFocus` on the SearchField is now disabled at mobile widths (`autoFocus={!isMobile}`), so opening the picker no longer raises the keyboard and the race has no trigger for the common path (open -> pick a favorite). Chosen over the bottom sheet because the project rule is to preserve existing appearance unless a redesign is approved. Covered by a smoke test that fails on mobile without the fix (a vitest version was written and DELETED — React Aria's autoFocus does not move document.activeElement under happy-dom, so it passed either way and proved nothing). REMAINING: tapping the search field still raises the keyboard, so the popover may still be displaced while typing — needs on-device confirmation. If it recurs, escalate to the bottom sheet (requires appearance approval). Deeper hypothesis worth testing then: `html, body, #root` are `position: fixed`, which breaks React Aria's assumption (calculatePosition.mjs:51) that an absolutely-positioned portal child of BODY resolves against the viewport, so the body's own `--app-top` offset may be double-counted. MANUALLY VERIFIED on-device after the partial fix: opening and using the mobile picker, including search/keyboard interaction, works; no bottom-sheet redesign needed.
- [x] Fix camera photos being stored rotated 90°. NOT an iOS bug — the cause is Alloy's own `maybe_downscale_image` in `alloy-server/src/routes/fs.rs`. Phone cameras store portrait shots as *landscape* pixels plus an `EXIF Orientation` tag (the standard behavior); that function calls `image::load_from_memory`, which decodes raw pixels and ignores the tag, then re-encodes via `write_to`, which drops all EXIF — leaving landscape pixels with no correction left. Only triggers above `MAX_IMAGE_DIM` (1568px), which every camera photo exceeds, so smaller images/screenshots keep their EXIF and look fine; this is why it presents as "camera photos are always rotated". Confirmed on `conversations/attachments/2026-08-01-0743-82b6-img-001.jpg`: stored 1568x1176 landscape, 0 EXIF keys, visibly sideways. Because the tag is destroyed at write time, EVERY consumer sees it rotated — the composer thumbnail, the message bubble, and all vision models — so this silently degrades image understanding, not just display. Fix: before resizing, read the orientation (`image::metadata::Orientation::from_exif_chunk`) and bake it into the pixels (`DynamicImage::apply_orientation`); both exist in the pinned image 0.25.10. Note it is a Rust/server fix, so it applies to desktop and mobile alike. Add a test with an orientation-tagged fixture asserting post-write pixel geometry. Existing attachments already lost their EXIF and would need a one-off repair if worth it. DONE: `maybe_downscale_image` now decodes via `ImageReader::into_decoder()`, reads `ImageDecoder::orientation()`, and calls `DynamicImage::apply_orientation()` before resizing, so the rotation is baked into the pixels the re-encode keeps. Sub-threshold images still return byte-identical with EXIF intact. Three tests added (orientation baked in; untagged images unrotated; small tagged images untouched); the first was confirmed to FAIL without the fix with the exact real-world symptom (`expected portrait, got 1568x1176`). Verified end-to-end through POST /api/fs/writeFile: 4000x3000 + Orientation=6 in -> 1176x1568 portrait out, pixels correct.
- [x] Don't silently drop images on text-only providers, and verify image support per CLI adapter. NOTE from testing: `claude-cli` images DO work — a red/blue PNG and an 11.3MB base64 phone photo both round-tripped correctly through the real stream-json protocol, and Claude honors EXIF orientation. So "Claude CLI doesn't work for images" is most likely the separate EXIF-rotation bug above (photos arrive sideways) and/or `flatten_conversation` (`cli_claude.rs:709`) sending images from only the most recent image-bearing user turn, so earlier images in a conversation are invisible to the model. `codex-cli` genuinely cannot accept images (`cli_codex.rs:422` hardcodes `images: vec![]`), but Alloy still lets you attach an image, sends the text alone, and gives no indication the attachment was discarded — so the model replies "no photo came through" and it reads as a broken upload. Observed in `conversations/2026-08-01-0743-82b6-...yaml`, where it cost four model switches (including a 402 on OpenRouter) before a vision-capable model answered. SECOND CONFIRMED REPRO — `conversations/2026-08-14-1110-aa24-whats-this-about.yaml` (attachment `2026-08-14-1110-aa24-img-001.png`, 1568x1176, stored fine): on `codex-cli/gpt-5.6-sol` the user asks "whats this about?" with an image attached, and rather than reporting a missing image the model CONFIDENTLY DESCRIBES ALLOY'S INJECTED CONTEXT BUNDLE instead ("Your identity, preferences, family cont..."), because the image was stripped and the bare question had no referent. Only when asked "can u see the image?" does it admit "No image is attached to this conversation." This makes the bug worse than a silent drop: the user gets a fluent, plausible answer about the wrong subject, and it also surfaces internal context/memory to the user as if it were their content. Any fix must prevent the send outright (or strip+warn) rather than relying on the model to notice. Add a `supports_images` provider capability alongside the existing `supports_tools`, expose it through `/api/models`, and have the composer surface it — disable the attach button and/or toast when the selected model can't accept images. Also consider warning when switching to a text-only model in a conversation that already has image attachments. DONE: added `Provider::supports_images()` (default true, false for codex), surfaced through `/api/models` as `supportsImages` — omitted from the wire when true so absence keeps meaning "supported". The composer disables the attach button with an explaining tooltip/aria-label, ignores image pastes, and warns when attachments are already pending and the model is switched to a text-only one. 2 Rust + 4 SPA tests; the SPA ones fail when the gate is forced open. FOLLOW-UP: Codex app-server image support has now shipped, so Codex no longer emits `supportsImages: false`; the generic capability gate remains for any future genuinely text-only provider.
- [x] Mobile: rotating portrait -> landscape breaks scrolling. Likely interaction with `useVisualViewport` (`src/hooks/useVisualViewport.ts`), which pins `--app-height`/`--app-top` from `visualViewport` while `html, body, #root` are `position: fixed` (`src/App.css:31-40`). An orientation change fires `resize` on a different axis and iOS reports stale/transitional viewport metrics during the rotation animation, so the cached height can be left describing the pre-rotation orientation. Reproduce at both orientations on a real device (Playwright cannot simulate iOS rotation metrics); consider re-measuring on `orientationchange` and after a settle delay, not just `resize`. FIX ATTEMPTED (NOT reproducible here — needs on-device confirmation): `useVisualViewport` listened only to `visualViewport` resize/scroll, so a rotation was measured once, at the moment it began. iOS reports transitional metrics during the rotation animation and does not reliably fire a final `visualViewport` resize once it settles, leaving `--app-height` describing the PREVIOUS orientation — and since `html, body, #root` are `position: fixed; height: var(--app-height)`, every scroll container beneath them gets the wrong extent, which is what makes scrolling appear broken rather than merely mis-sized. Now also listens to `orientationchange` (plus window `resize` as a backup) and re-reads across the animation at 50/150/350/600ms, writing only on change to avoid layout churn. 4 unit tests; the orientation one fails without the fix with the exact symptom (`expected '800px' to be '390px'`). CAVEAT: the re-read schedule is inferred, not measured against a real device — if rotation still breaks scrolling, the animation likely outlasts 600ms, and the next step is to log `visualViewport.height` over time on-device to find the real settle point rather than extending the delays blindly.
- [x] Phones in landscape get the DESKTOP layout. The mobile breakpoint is `@media (max-width: 768px)`, but an iPhone in landscape is ~874 CSS px wide, so every mobile rule is skipped on a viewport only ~400px tall. Surfaced as the oversized composer (fixed 200px cap = half the screen, fixed separately by capping at 35% of --app-height), but the same mismatch applies to the whole composer row, touch-target sizing, and the sidebar/detail split. Decide the intended contract — probably orientation- or height-aware (`(max-height: 500px)` and/or `(pointer: coarse)`) rather than width alone — rather than patching each symptom. Verify on a real device in both orientations. DONE: the mobile contract is now `(max-width: 768px), (max-height: 500px) and (pointer: coarse)` — narrow viewports OR short TOUCH viewports — applied across all 8 feature stylesheets and mirrored by `MOBILE_MEDIA_QUERY` in `useIsMobile.ts` (plain CSS can't share a named query, so the two copies must move together). Verified in WebKit across six shapes: iPhone portrait/landscape and iPad portrait → mobile; iPad landscape (1024x768, touch but tall), desktop, and a SHORT desktop window (1440x400, fine pointer) → desktop, so neither large touch devices nor resized desktop windows regressed. NOT changed: `Dialog.module.css`'s separate 600px bottom-sheet breakpoint — a bottom sheet on a ~400px-tall landscape viewport would be worse than the centered dialog, so it stays width-only deliberately.
- [x] Mobile: tapping an external link leaves Alloy in a broken half-dismissed state that needs manually X-ing out. The link does open, but the Alloy page is left partially covered/scrolled and unusable. Check the link handling path (`src/utils/wikiLinks.ts` uses `openUrl` from the opener shim; `src/services/api/tauri-opener.ts` falls back to `window.open` in the browser). In a standalone/home-screen PWA-style context on iOS, `window.open` opens an in-app browser overlay whose dismissal can leave the page scrolled with a stale `--app-top`/`--app-height`, matching the "half down" symptom. Confirm whether it reproduces in Safari vs added-to-home-screen, and re-measure the visual viewport on `pageshow`/focus after returning. CAUSE FOUND (reported for Apple Music links specifically, e.g. https://music.apple.com/us/song/1651330689 — that detail was the key): those are UNIVERSAL LINKS, which iOS hands off to the native app. `wikiLinks.ts` called `preventDefault()` then `openUrl()`, which in a browser is `window.open(...)`. That converts a native anchor activation into a PROGRAMMATIC popup, so iOS opens a separate tab and performs the app handoff from that tab, stranding it — returning to Safari leaves Alloy behind a blank tab you must close by hand, the reported 'half-dismissed' state. Ordinary https links just open a tab you can dismiss normally, which is why it only showed up for app-handoff links. FIX: in a browser, don't intercept at all — render a plain `target=_blank rel=noopener noreferrer` anchor and let Safari handle the handoff (this also restores middle-click and modifier-click, which the old onAuxClick swallowed). The desktop shell still intercepts, because an unhandled navigation there would load the page inside the webview and replace Alloy. 2 tests covering both runtimes; the browser one fails under the old always-intercept behavior. NOT verifiable here — no headless browser performs iOS universal-link handoff — so confirm on-device with an Apple Music link.
- [x] Mobile: remember the last-open conversation for longer. Selection currently does not survive long enough across app suspension/reload, so returning to Alloy drops you out of the conversation you were reading. Establish where selection is persisted (`selectedItem` in App.tsx vs `localStorage`) and give it a durable, longer-lived restore path, including after an iOS tab eviction. Coordinate with the watcher-resync task above so a restored selection still re-hydrates its messages. DONE: the selection was persisted in `sessionStorage`, which is scoped to a tab session and discarded when that session ends — precisely what iOS does to a backgrounded tab and what a desktop app restart looks like, so it was lost exactly when restoring it mattered. A plain reload PRESERVES sessionStorage, which is why this never showed up in ordinary testing. Moved to localStorage behind `src/services/selectionStorage.ts`, with a one-time read of the old key so an in-flight session isn't disrupted, and validation so a corrupt value returns null instead of breaking startup. App.tsx's mobile-view restore read the same key separately and now shares the module, so the two can't disagree. Trade-off accepted: two windows on one machine share a last-selection rather than each keeping its own. 7 unit tests plus a smoke test that clears sessionStorage before reloading — it fails when reverted to sessionStorage.
- [x] `flux-highlights` skill produces wrong/no results under `codex-cli`. Skills are delivered via the SYSTEM PROMPT (`streaming.rs:402` `apply_invoked_skill`), and `cli_codex.rs` `flatten_prompt` DOES prepend the system text — so the plumbing is present and the cause needs real diagnosis rather than assumption. Leads: `codex exec` runs its own coding-agent loop with its own framing in a read-only sandbox rooted at a temp dir, which may override or ignore an injected persona/output-format instruction; `supports_tools` is false for codex, so anything relying on the `use_skill` tool path rather than the slash-command path is unavailable. Deliverable: determine whether the skill text actually reaches the model (log the flattened prompt), then either adapt the codex adapter or clearly mark skills unsupported for it. MEASURED (codex-cli, use_skill invocations counted server-side via a new `mcp tool call:` log line, 5 runs per case): with the CURRENT catalog wording codex calls `use_skill` 0/5 on a task that plainly matches a skill — it improvises from the one-line description and produces the wrong format, which fully explains the reported bug. The plumbing is FINE: told explicitly to call the tool, codex calls it over MCP and gets the real instructions back verbatim (v0.4.6 made this reachable). The SLASH COMMAND path is reliable — `/flux-highlights` injects the skill into the system prompt and produced the exact required format every time. A stronger catalog wording ("descriptions are summaries only, you MUST call use_skill first, never work from the description alone") raises it to only 2/5, so prompt wording ALONE IS NOT A FIX and was not shipped. Notably it does NOT over-trigger: 0/5 false fires across arithmetic, a general question, and two skill-adjacent asks ("rewrite this shorter", "summarize this"), so over-eagerness is not the obstacle — under-eagerness is. Options to weigh: (a) document the slash command as the supported path for codex and leave the tool route best-effort; (b) inject the full skill text when the user names a skill, as the slash command already does; (c) for CLI adapters, inline all skill instructions in the system prompt instead of requiring a tool call, trading tokens for determinism. CLOSED AS ACCEPTED BEHAVIOR: explicit `/skill-name` invocation is the deterministic supported path; autonomous catalog-to-`use_skill` selection remains best-effort model behavior. No Codex-specific prompt enforcement, automatic retry, or token-heavy eager skill injection will be added unless user expectations change.
- [x] Search should be full-text across conversations. It currently behaves as title-only for chats, but the code already TRIES to do full text: `Sidebar.tsx:386-391` searches `item.conversation.messages`. Since the startup optimization, conversations are loaded as metadata-only summaries with `messages: []` (`loadConversationSummaries`), so that loop runs over an empty array for every conversation not opened in the current session — the feature is dead code rather than missing code, which is why it looks inconsistent (a conversation becomes searchable once you open it). Notes and riffs DO work, because `loadNotes` reads bodies. Do NOT fix by loading all message bodies client-side: the real vault is ~198MB across ~2200 conversations, and eagerly loading it would undo the 7s->0.5s startup win. Add a server-side search endpoint in alloy-server that scans conversation YAML (the Rust side already owns the vault and can stream/grep without materializing everything), have the sidebar query it with the existing 200ms debounce, and merge those hits with the current client-side title/notes matching. Include match context in the result so the sidebar can show a snippet. Verify against the real vault for both latency and relevance. DONE: added `GET /api/search?q=` (`alloy-server/src/routes/search.rs`) scanning conversations/, notes/ and riffs/ next to the files, returning typed hits (kind, id, title, snippet) that the sidebar merges with its existing title/notes matching. Reuses the snippet helpers from `tools/search.rs` rather than duplicating them. Uses a 512KB read cap per file and scans every eligible file. The original 300-hit global cap was removed after review because conversations were scanned first, so a broad query could prevent notes and riffs from being searched at all. Results now include every matching id and the already-recency-sorted timeline controls display order. Measured on an 86MB / 2200-conversation corpus: 0.24s for a broad query matching every file and ~1.0s for a sparse/no-match query requiring full reads; expect roughly twice that on the ~200MB real vault. The sidebar renders the returned context snippet, identifies riffs separately from notes, clears matches while a new request is pending, and rejects late responses from superseded queries. Conversation titles/ids are read from the YAML header without parsing the message array, stopping at `messages:` so a `title:` inside a message can't be mistaken for the header field. 12 Rust + 3 hook unit tests + 1 smoke test. Note bodies are no longer shipped to the client; they load only when opened. FOLLOW-UPS: (a) matching is case-insensitive substring only, no multi-term/fuzzy, unlike the AI-facing `search_directory` tool; (b) every query now costs a full scan for correctness — an index would fix that if it becomes noticeable. CLIENT RESPONSIVENESS FOLLOW-UP DONE: the controlled field now owns its live value in a small child component and only updates the large Sidebar after the existing 200ms debounce. Previously every keystroke reconciled the full unvirtualized ~2,200-row timeline even before a server request began. A regression test counts row formatting work: it fails against the old implementation and confirms rows stay untouched until the debounce expires. No server cache/index or debounce increase was added.
- [x] Delete `src/services/imageUtils.ts`'s `compressImageToFit` + `imageUtils.test.ts` — DONE: the entire module was dead (including `mimeTypeFromPath`; no production imports) and is superseded by the EXIF-aware server-side downscaler in `alloy-server/src/routes/fs.rs`.
- [x] Surface subscription-CLI version skew. Anthropic gates which models it serves on the Claude Code CLI's release channel, so an Alloy host on the `stable` cask (2.1.212 -> resolves `opus[1m]` to `claude-opus-4-8[1m]`) silently offers fewer/older models than one on the fast channel (2.1.220 -> `claude-opus-5[1m]`), with no visible signal — discovery is working correctly, it just looks stale. The valuable signal is disagreement *between hosts* (laptop vs the always-on box serving alloy.z3.ca), not "your CLI is old", since staying on stable for an unattended host is a deliberate choice. Record the resolved CLI version alongside discovered models (Claude reports it in the `list_models` control response path; Codex in app-server `initialize`), expose it in Settings next to each CLI provider, and consider warning when a model key resolves to different underlying models across hosts. Keep it informational — do not auto-update or block on a version check. CLOSED AS NOT PLANNED: the authenticated model picker already exposes the account- and policy-filtered catalog that can actually be used. A raw CLI version neither explains availability nor compares hosts automatically, and in browser mode it would describe the remote Alloy server rather than the device in hand. Keep version/model protocol probes as troubleshooting diagnostics; add a dedicated diagnostics surface only if a concrete recurring support need appears. NOTE: Claude model discovery returns resolved model ids but not the CLI version; that requires a separate `claude --version` probe.
- [x] `tauri-action` reports a spurious `Unknown error: {}` after a SUCCESSFUL upload, failing the release job. Seen on v0.4.8 (run 31914485025): the Intel build compiled, signed, notarized (Accepted), bundled and uploaded every asset — `gh release view` confirmed all four tarball/sig assets present — then the step errored while finishing `Alloy_x64.app.tar.gz.sig`. Reproducible: a `--failed` rerun died identically, so this is not flake. Because the job is marked failed, `publish-release` and `deploy-updater` are SKIPPED, leaving the release as a draft and `latest.json` on gh-pages stale — i.e. a fully built release silently does not ship. Worked around by hand for 0.4.8 (downloaded the release assets, rebuilt latest.json in the workflow's exact format, force-pushed gh-pages, published the draft), which is fragile: a mistake there breaks updates for every client. Fix: pin or upgrade `tauri-apps/tauri-action` (also see the Node 20 item — do both together), or stop relying on it to upload and attach assets in a separate step whose failure is distinguishable. Validate on a throwaway tag before the next real release. CLOSED: retain the existing single `tauri-action` release topology. The complete v0.4.13 release validated both signed/notarized architectures, artifact publication, and updater deployment without recurrence. There is no justified proactive change; reopen with fresh logs only if the post-upload failure returns.
- [x] Upgrade GitHub Actions off Node 20 before the compatibility shim is removed. The v0.4.5 release run warned: "Node.js 20 is deprecated. The following actions target Node.js 20 but are being forced to run on Node.js 24: actions/github-script@v7" (https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/). Today it still runs, forced onto Node 24; when GitHub drops the shim it will fail outright. Flagged: `actions/github-script@v7` in `.github/workflows/release.yml`. Audit the rest at the same time — `actions/checkout@v4`, `actions/setup-node@v4`, `actions/upload-artifact@v4`, `actions/download-artifact@v4` (both workflows) and `peaceiris/actions-gh-pages@v4` are also Node20-era majors; check each action's current major rather than assuming a version number. RISK: `release.yml` only runs on tag push, so a mistake here is discovered at the worst possible moment (mid-release, after a version bump commit and tag already exist). Validate on a throwaway tag (e.g. `v0.0.0-ci-test`, deleted afterwards) or via `workflow_dispatch` BEFORE relying on it for a real release; confirm the draft release, both macOS artifacts, the generated `latest.json`, and the gh-pages publish all still work end to end. `ci.yml` is lower risk and can be done first as a canary. DONE: upgraded checkout v4->v7, setup-node v4->v7 with explicit Node 24, upload-artifact v4->v7, download-artifact v4->v8, and github-script v7->v9. Verified each target action declares `node24`; `peaceiris/actions-gh-pages@v4`, `Swatinem/rust-cache@v2`, and `tauri-action@v0` already did and were left in place. No release topology change. Canary CI run 32072260992 passed checks/Rust/smoke, then v0.4.13 run 32075270888 passed both signed+notarized architecture builds, artifact upload/download, publication, and updater deploy; the public latest.json serves 0.4.13.
- [x] Codex follow-ups: real token streaming from `codex exec --json` (currently one-shot, final message only), mid-run cancellation (kill the child — currently only pre-flight cancel), image support (`-i/--image`), TOOL ACCESS IS DONE (see below); still open here are streaming, cancellation and images. Tool access via MCP — `codex mcp add/list/remove` manages servers and `-c mcp_servers.<name>...` can inject one per `codex exec` run, so Alloy's existing MCP server (`alloy-server/src/routes/mcp.rs`, already used to give claude-cli its tools) should be attachable the same way. That would lift the current `supports_tools() == false` for codex. TOOL ACCESS SHIPPED: `base_command` now attaches Alloy's MCP bridge via `-c mcp_servers.alloy.url="<base>/api/mcp?session=..&token=.."`, the same endpoint claude-cli uses, so codex tool calls dispatch through the identical `ToolRegistry::execute`. Two non-obvious findings: (1) Codex gates MCP tool calls behind an approval prompt — under plain `exec` nobody can answer it, so every call is auto-cancelled and the model connects and lists tools but never invokes one, reporting "the request was canceled"; `--approve-for-me` routes approvals through Codex's own automatic review and fixes it. (2) `--approve-for-me` is mutually exclusive with `--sandbox` and implies workspace-write, so the read-only sandbox is now applied only on turns WITHOUT tools; containment comes from `command()` running codex in `std::env::temp_dir()`, so its native tools only touch scratch space while the vault is reachable solely through Alloy's scoped MCP tools. Deliberately avoided `--dangerously-bypass-approvals-and-sandbox`. Verified end to end: codex read a vault-only secret through Alloy's MCP endpoint and returned it. DONE: probing codex-cli 0.147 showed `exec --json` fundamentally emits only a completed message, while app-server exposes `item/agentMessage/delta`, `turn/interrupt`, image/data-URL inputs, token usage notifications, and MCP item events. Interactive turns now use one ephemeral app-server thread/process: real token deltas stream into the existing UI; Stop sends `turn/interrupt` with a 3s kill backstop and preserves partial text; vault-resolved base64 images are attached as data URLs (no vault path exposure); Alloy MCP config and automatic approval review are preserved; MCP use/results surface as normal Alloy tool pills. `codex exec` remains only for title generation/compaction. Verified through the REAL Alloy HTTP/SSE path against codex-cli 0.147: text produced 4 incremental chunks; a generated red PNG was answered “Red”; `read_file` returned a vault-only secret with tool_use/tool_result events; cancellation ended in 0.03s with `stopReason: cancelled` and partial content. 16 adapter tests include fake-process streaming and active cancellation. TOOL-PILL FOLLOW-UP DONE: two independent gaps explained the missing indicators. The SPA's `addToolUse` changed the internal streaming map without advancing the context version, so React did not render a pill until an unrelated update; it now notifies consumers immediately, with a regression test confirmed failing against the old implementation. The app-server adapter also filtered item events to `mcpToolCall`, hiding Codex-native `commandExecution`, `webSearch`, `fileChange`, and `dynamicToolCall` activity; all now translate into the same SSE/persistence shape, with file patches reduced to path/kind metadata. Verified in a real browser through Alloy: an MCP `read_file` pill appeared live as “Reading” and remained after completion as “Read file”; a real native `pwd` command emitted tool_use/tool_result and persisted `command_execution` in YAML. WEB-SEARCH LABEL FOLLOW-UP DONE: real Codex `item/started` notifications contain `query: ""`; the actual query arrives only in `item/completed`. Tool calls are now updated by app-server id across server history, SSE reconnection state, and live React state, so one generic “Searching” pill becomes one query-labeled completed pill rather than persisting `Searched ""` or duplicating. Existing empty-query history renders as plain “Searched.” Verified through the real browser/app-server path: `Searching` -> `Searching "Alloy Tauri AI chat GitHub"` -> one final `Searched` pill, with the query persisted in YAML. MESSAGE-BOUNDARY FOLLOW-UP DONE: Codex emits commentary and final output as distinct `agentMessage` items, but neither item supplies boundary whitespace; concatenating their token deltas produced persisted text such as `input.That confirms`. Interactive streaming now tracks `itemId` and inserts a paragraph break only when the agent-message item changes and neither side already has whitespace. A regression test was confirmed failing with the separator disabled, and the real Alloy HTTP/SSE path persisted commentary and final answer separated by `\n\n`. Existing malformed vault history is not rewritten automatically.

<!--
UI foundation migration (prototype-validated). Approach: React Aria Components for
behavior/accessibility + CSS Modules for styling + a semantic design-token layer;
expose only Alloy-owned wrappers to feature code. Do these in order (each depends on
the previous), strangler-fig style: keep the 98 vitest + Playwright e2e suites green,
keep Tauri and web/server modes working, and delete superseded CSS/hooks only as each
consumer migrates. Validated in WebKit (Safari engine): popover placement, upward
positioning, focus trapping, dialog Escape, and disclosure visibility all pass.
-->

<!--
MIGRATION STATUS (11 commits on main: 93cf6c1 .. d5a281c). All verified with
tsc + eslint (0 errors) + 121 vitest tests (incl. real user-event interaction
tests) + web build; interactions cross-checked in WebKit.

DONE + committed:
  step 1 tokens + ui/ primitives (Button, Dialog, SearchField, SelectField, Menu)
  step 2 theme (light/dark/system, matchMedia, Appearance control); DEFAULT IS
         STILL 'light' on purpose (see src/theme.tsx DEFAULT_PREFERENCE) — do not
         flip to 'system' until step 8 tokenization is complete, or dark looks broken.
  step 3 ModelSelector rebuilt (DialogTrigger + Popover + Autocomplete + ListBox).
         Fixes made after review: (a) favorite star was inert because RA caches rows
         by key — bake `favorite` into each row object; (b) re-selecting the current
         model didn't dismiss — drive picking with ListBox onAction (NOT
         onSelectionChange / selectionMode, which only fires onAction on dbl-click),
         highlight current row via .optionCurrent class. Star = span role=button,
         tabIndex -1, onPointerDown preventDefault+stopPropagation, onClick toggles.
  step 4 all modal dialogs -> AlloyDialog (extended with isOpen/onOpenChange for
         parent-controlled dialogs): Settings + reset, Sidebar rename/delete, riff modal.
  step 5 ContextUsageChip -> DialogTrigger+Popover. DECISION: SlashCommandMenu and
         right-click ContextMenu stay custom (textarea-driven keyboard / no RA
         primitive) and are only tokenized, not rebuilt.
  step 7 riff change review -> RA Disclosure.
  partial step 8: legacy-var aliases in tokens.css tokenize ~130 usages app-wide;
         App.css shell tokenized; dead useClickOutside removed (useGlobalEscape stays).

  step 8 tokenization DONE: every feature + secondary stylesheet now maps
         surface/text/border/accent/status hex to tokens; verified sidebar, chat,
         markdown, and settings in dark via CSS fixtures. Remaining hardcoded hex are
         intentional (badge gradients, status indicators, search-highlight yellows,
         syntax colors, the dark Toast overlay, favorite-star colors).

DEFERRED (product decision, not a bug):
  Theme DEFAULT is still 'light' (src/theme.tsx DEFAULT_PREFERENCE). Now that dark
  mode is visually complete, flipping the default to 'system' is safe but changes
  default appearance for dark-OS users — confirm before flipping.

LESSON: every migrated component needs a real @testing-library/user-event interaction
test (open overlay, click, keyboard), not just a closed-trigger render test — the star
bug and the re-select bug both slipped past render-only tests.

NOTE: pre-existing uncommitted work (Ollama removal / Claude CLI changes across
alloy-server + a few TS/docs) and .claude/settings.local.json are intentionally NOT
committed — keep excluding them; commit UI work with partial-staged package.json.
-->

- [x] UI foundation, step 1 — add the design-system scaffolding without migrating any feature yet. Add the `react-aria-components` dependency (^1.19). Create `src/styles/tokens.css` with semantic tokens (canvas/surface/surface-raised/surface-hover, border/border-strong, text/text-secondary/text-muted, accent/accent-strong/accent-soft, danger/success/warning + soft variants, focus ring, overlay, chip/track/code-bg/added/control-track/scrollbar, radii, shadows, typography, motion, and app metrics) under `:root` (light), plus a `:root[data-theme="dark"]` block overriding the color tokens for dark. Add `src/components/ui/` primitives as thin Alloy wrappers over React Aria + CSS Modules: `Button` (variants primary/secondary/quiet/danger; sizes small/medium/icon/composer), `Dialog` (ModalOverlay+Modal+Dialog with title/close, compact+bottom-sheet-on-mobile), `SearchField`, `SelectField`, `Menu` (Popover+Menu). Do NOT rewire any feature yet; add a render test that mounts each primitive. Note the React Aria state-attribute gotchas found in the prototype: ComboBox root exposes `data-focused` (not `data-focus-within`); SearchField exposes `data-empty` (not `data-has-value`); a DisclosurePanel needs an explicit `.panel[hidden]{display:none}` rule to hide in WebKit.
- [x] UI foundation, step 2 — theming. Add `src/theme.tsx` (ThemeProvider + useTheme) with a `light | dark | system` preference; resolve `system` via `matchMedia('(prefers-color-scheme: dark)')`, apply the result to `document.documentElement.dataset.theme`, and re-resolve on OS change while in system mode. Wrap the app in `main.tsx`. Add an Appearance section to Settings with a Light/Dark/System segmented control wired to `useTheme`. Depends on step 1. Dark mode will only be visually complete for components that already consume tokens; that is expected and finished in step 8. Verify the attribute flips and system detection works; keep existing look unchanged in light mode.
- [x] UI foundation, step 3 — migrate `src/components/ModelSelector.tsx` to the prototype-validated picker and delete its bespoke dropdown logic. Replace the editable-combobox behavior with a trigger `Button` that opens a `Popover` containing React Aria `Autocomplete` (a `SearchField` + a `Menu` with `selectionMode="single"`), so the whole control opens/toggles on click (no text-selection on focus), the search field lives inside the popover, the chevron sits inside the trigger, and selecting closes + updates. Preserve all current behavior: dynamic model catalog, favorites toggle as an independent hit target inside each row, stale-model humanized fallback, relevance ranking, empty states, provider tags, and the local/loopback lock treatment. Style with a `ModelPicker.module.css` using tokens. Remove now-dead state/effects (open state, active index, arrow-key handling, outside-click, above/below flip) and any `useClickOutside` usage this file introduced. This slice is also the manual checkpoint: smoke-test in the real Tauri (WKWebView) shell AND web/browser mode, including upward popover placement and the on-device software keyboard, before proceeding.
- [x] UI foundation, step 4 — migrate modal dialogs to the `Dialog` primitive and remove the duplicated overlay code. Convert `src/components/Settings.tsx` (main dialog + reset-confirm), the Sidebar rename and delete dialogs in `src/components/Sidebar.tsx`, and `src/components/RiffBatchApprovalModal.tsx` to `components/ui/Dialog`, getting focus trap, focus restoration, Escape, and background inertness for free. Delete the superseded `.settings-overlay/.settings-dialog/.rename-modal/.rename-dialog/.riff-modal-*` CSS and the `useGlobalEscape`/manual `stopPropagation` overlay patterns those files used, where fully replaced. Depends on step 1.
- [x] UI foundation, step 5 — migrate lightweight overlays/menus. Migrated `ContextUsageChip` to a React Aria `DialogTrigger` + `Popover` (removing its manual open state and outside-click effect) and tokenized its CSS. Decision on the other two: `SlashCommandMenu` stays custom — its keyboard nav is driven by the composer textarea's keydown (arrows/enter/tab intercepted before submit), which doesn't fit the `Menu` primitive's focus model; it's tokenized in step 8 instead. The right-click `ContextMenu` also stays custom (React Aria has no context-menu primitive; it's already a coordinate-positioned overlay) and is tokenized in step 8. Depends on step 1.
- [x] UI foundation, step 6 — extract a shared `ItemHeader` pattern (back button + title/subtitle + actions slot) under `src/components/` and adopt it in the chat, note, task, and riff headers (`ItemHeader.tsx` already exists — align it to the token/primitive system and the wrapper Button). Migrate the composer control row (`ChatInputForm` and the riff composer) to shared `Button` sizes with a single 48px desktop rhythm and >=44px touch targets, textarea aligned to the same baseline, and the send button pinned bottom-right on mobile. Keep `useAutoResizeTextarea`/`useTextareaProps` behavior. Depends on steps 1 and 3. DONE: all chat/note/task/riff surfaces already used ItemHeader; its built-in back/forward/settings/close controls now use the shared React Aria Button with the established 32px/28px geometry, accessible names, and optional subtitle support. Note/task/riff header actions use shared small variants. Chat and Riff attach/mic/send controls use the shared 48px composer size while preserving textarea auto-resize, model-picker alignment, recording animation, disabled states, and mobile ordering. Removed the superseded per-feature header/button/composer CSS. Before/after desktop and 390px mobile screenshots were visually equivalent; 16 focused interaction/render tests, the full verification pipeline, and 21 smoke tests pass.
- [x] UI foundation, step 7 — migrate the riff surface patterns. Convert `InterventionCard` to a token-styled pattern (type-colored left border + glyph + dismiss) and the integration review (`RiffBatchApprovalModal`, already on the Dialog primitive from step 4) to use `Disclosure` for per-change expand with a diff-style additions block and reasoning. Depends on steps 1 and 4.
- [x] UI foundation, step 8 — completion sweep. Tokenized hardcoded colors across every feature + secondary stylesheet so dark mode is visually complete; removed the orphaned `useClickOutside` hook. Verified sidebar/chat/markdown/settings in dark via CSS fixtures; 121 vitest tests pass. Intentional exceptions kept (badge gradients, status colors, search highlights, syntax colors, dark Toast overlay, star colors). NOT included: flipping the theme default from 'light' to 'system' (deferred — see status block).

- [x] UI polish — replace the sidebar type-filter `<select className="filter-dropdown">` in `src/components/Sidebar.tsx` with a segmented tab control (All / Chats / Notes / Tasks / Riffs), matching the React Aria prototype. Reuse the tokenized segmented-control pattern from the Settings Appearance control (`.settings-theme-group`), or a React Aria `ToggleButtonGroup`, styled with tokens. Preserve the existing `TimelineFilter` values, `onFilterChange` behavior, and active-filter highlight; support keyboard arrow navigation. Remove the now-unused `.filter-dropdown` CSS. The five tabs must fit the ~280px sidebar and degrade acceptably on mobile (wrap or horizontal scroll).
- [x] UI polish — add a visible settings affordance (gear in the header, via a SettingsLauncher context; done).
- [x] UI polish — add a `ui/Switch` primitive (React Aria `Switch`) and migrate the Settings “Share on network” toggle to it (done).
- [x] UI polish — migrate ThinkingDisclosure to a React Aria `Disclosure` (done; keyboard/ARIA, `[data-expanded]` chevron, `[hidden]` panel rule; interaction-tested).
- [x] UI polish — add a `ui/Tooltip` (React Aria `Tooltip` + `TooltipTrigger` + `Focusable`) and apply it to the header settings/close buttons and the composer attach/send buttons (done; tokenized, interaction-tested). Follow-up if wanted: extend to the header back/forward buttons (dynamic/disabled) and the model picker.

## Definition of done — mobile reliability push

Every item needs BOTH an automated check (committed with the fix, runs in CI) and a
manual on-device pass. Neither alone counts as done. Where the automated check is a
structural PROXY rather than a true reproduction, it says so — those rely on the manual
pass for real confidence, and the proxy exists to stop silent regression later.

| # | Item | Automated (committed + CI) | Manual (real iPhone) |
|---|------|----------------------------|----------------------|
| 1 | Watcher resync | Smoke: write a conversation while the backend is bounced -> it appears after reconnect without reload. Unit: merge keeps `messages`/`messagesLoaded` for the open conversation and re-invalidates only when `updated` changed. Unit: `ensureWebSocket()` rejects (never hangs) when a connect fails while another caller waits. | Open a conversation, lock the phone 2+ min, let a scheduled task or desktop edit land, unlock -> change appears within seconds AND the conversation you were reading still has its messages. |
| 2 | EXIF rotation | Rust: orientation-tagged fixture through `maybe_downscale_image` -> output geometry is portrait and pixel probe matches the oriented image. Rust: images at/below `MAX_IMAGE_DIM` still pass through byte-identical. | Take a PORTRAIT photo -> upright in composer thumbnail, upright in the sent bubble, and the model describes it upright. Repeat once in landscape. |
| 3 | Text-only image drops | Rust: provider capability is surfaced to the composer; Codex and Claude now both report image support. Synthetic text-only model tests keep the blocking/warning behavior covered. | Attach a photo under codex-cli and claude-cli -> both accept and understand it. A future provider advertising `supportsImages: false` must still block before send. |
| 4 | Portrait -> landscape scrolling | PROXY (Playwright cannot reproduce iOS viewport metrics): swap viewport w/h + dispatch `orientationchange` -> transcript stays scrollable (`scrollHeight > clientHeight`, `scrollTop` changes), composer visible, no zero-height panes. | Scroll mid-conversation, rotate to landscape -> scrolling works, no dead zones, composer reachable; rotate back -> still fine. Repeat with the keyboard open. |
| 5 | Link half-dismissed state | PROXY (no in-app-browser overlay in Playwright): unit test that returning focus (`pageshow`/`focus`/`visibilitychange`) re-measures the visual viewport and resets `--app-height`/`--app-top` to current values. | Tap an external link, return to Alloy -> normal state, no X-out needed. Test BOTH Safari and added-to-home-screen. |
| 6 | Remember last conversation | Smoke: select a conversation, reload -> same conversation restored with messages. Unit: persisted selection survives a simulated long gap (no short TTL eviction). | Open a conversation, background Alloy 30+ min (long enough for iOS to evict the tab), reopen -> same conversation, sensible scroll position. |
| 7 | flux-highlights on codex-cli | Rust: `flatten_prompt` with a System message containing skill text -> the skill text is present in the flattened prompt (proves delivery). Then a cause-specific test once diagnosed. | Run `/flux-highlights` on a real highlights note under codex-cli -> output matches the skill's one-line format, and matches claude-cli's shape. |
| 8 | Startup speed (believed fixed, v0.4.4) | Smoke: with `/api/models` artificially delayed, the first timeline item still renders (deterministic: proves discovery is off the critical path, no timing flake). | Cold launch on cellular -> usable in about a second, sidebar populated. |
| 9 | Model picker stranded behind iOS keyboard (CONFIRMED on-device; re-filed, NOT the v0.4.4 fix) | Existing smoke still applies (empty catalog must not blank the picker). PROXY for the keyboard race (Playwright cannot reproduce it — `setViewportSize` fires resize synchronously and changes `innerHeight`): if fixed via bottom sheet, assert the picker uses the sheet at narrow widths and that its list is within the visual viewport; if fixed via `autoFocus` removal, assert the search field is NOT focused on open at narrow/touch viewports. | Open the picker on a NEW conversation (worst case — largest keyboard shift) and on a long conversation: the model list must be fully visible and scrollable above the keyboard, never only the search box. Repeat after foregrounding. |

If 8 or 9 fail the manual pass, they are NOT the bug that was fixed in v0.4.4 — re-file
as a new item with the observed symptom rather than reopening the old one.

## Future epics (not scheduled — no `- [ ]`, so the runner skips them)

- **Plugin architecture** — return Alloy to an extensible, plugin-oriented app.
  Core model: **Resource → Workspace → Host capabilities**. Dogfood the extension
  API with first-party Notes, Tasks, Comparison, Council, Riff, and eventually
  Chat modules; add an **MCP client** for external capabilities and declarative
  vault packs as the no-code on-ramp. Full design, boundaries, trust model, and
  sequencing in [docs/plugin-architecture.md](docs/plugin-architecture.md).
