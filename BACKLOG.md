# Backlog

Tasks for the autonomous backlog runner (`scripts/backlog-runner.sh`).
Each `- [ ]` is one self-contained task. The runner picks the first open one,
works it on an `auto/<slug>` branch, verifies, and opens a PR for review.

Keep tasks small and unambiguous — one PR's worth of work each. Completed work
and the mobile-reliability checklist are archived in
[`docs/backlog-archive.md`](docs/backlog-archive.md).

<!-- Tier 1: observed correctness failures and their prerequisites. -->
- [ ] Warn before a conversation's private context is handed to a cloud provider. A conversation's model can be switched at any time, and the new provider receives the existing history — including tool results read from a `private/` mount — with nothing said about it. Going from a local model to a cloud one is therefore a one-click disclosure of whatever the local model was trusted to read, and it is silent. Decide from the conversation's own persisted tool history whether any turn resolved a local-only mount, and on a switch from a local to a cloud model require an explicit confirmation naming the provider that will receive it and what it contains. Warn, do not block: it is the user's data and sometimes they will mean it. Declining must leave the selected model unchanged rather than half-applying. Say nothing for a conversation with no private-mount history, for cloud-to-cloud or cloud-to-local switches, or when the provider is trusted-local, so the warning keeps its meaning. Cover each of those cases plus a conversation whose private read happened many turns earlier, and the decline path.
- [ ] Stop cloud models reading conversations that contain private-mount material. A local model may read `private/` mounts, and every turn is then persisted to `conversations/` as YAML plus its Markdown twin — which is an ordinary vault directory every model can read. Observed on 2026-09-16: a cloud model could not reach `private/obsidian_vault/Sarah relationship log.md`, so it searched `conversations/`, read seven files a local model had written, and reconstructed the note's path, title, and themes. The mount ACL held; persistence laundered around it. Mark a conversation at write time when a turn successfully resolves a local-only mount — the backend already knows, and `ToolContext.memory_read_this_turn` is the pattern to copy — persisting the flag near the top of the file so the check stays a cheap head-read across ~1,300 conversations, in the same atomic write as the turn. For cloud callers, marked conversations must be invisible to `read_file`, `search_directory`, and `list_directory` alike, returning the same generic not-found the mounts use: search snippets return content, and conversation filenames are title slugs, so blocking only whole-file reads leaves both content and subject matter exposed. Taint must propagate, or laundering simply takes one more hop. Provide a one-time backfill as an explicit command rather than a silent startup migration; on the reference vault about 20 of 1,359 conversations qualify. This is worth building even though the vault-partition epic would subsume it, because the provenance marking is what a later migration needs to decide where each conversation belongs.
- [ ] Make the skills the app advertises the same ones it can actually run. The SPA compiles the repo's `skills/*/SKILL.md` into the bundle and lists all seven under `# Available Skills` in the system prompt, while the Rust `SkillRegistry` scans only `<vault>/skills/` — so every bundled skill (`note-query`, `note-capture`, `note-linker`, `read-url`, `save-memory`, `summarize-note`, `create-scheduled-task`) is advertised to the model but cannot execute. `use_skill` answers `Unknown skill: note-query. Available skills: <the vault's>`, and the model improvises around it mid-answer, narrating the failure to the user; the vault has instances on 2026-05-13, 2026-08-25, and 2026-09-16, so this has been failing quietly for months. The slash path fails worse because it fails silently: the SPA accepts `/note-query` since its own registry knows the name, passes it as `invoke_skill`, and `apply_invoked_skill` drops unknown names without comment, so the turn runs with no skill and no sign that anything was missed. Load the same bundled definitions in the backend, embedded in the binary at compile time rather than read from a `skills/` path that only exists in a dev checkout, and keep vault skills overriding bundled ones by name so a user can still customize one. Vault-only skills must behave exactly as they do today. Make an unknown slash-invoked skill visible instead of silent. Cover a bundled skill resolving through both `use_skill` and slash invocation, a vault skill shadowing a bundled one of the same name, an unknown name still erroring, and — the point of the exercise — a parity test asserting the set the SPA advertises equals the set the backend can execute, so the two cannot drift apart again.
- [ ] Let the model continue reading past a truncated tool result. `read_file` caps output at 64 KB (`MAX_READ_BYTES`) and appends `[truncated: file is N bytes; showing the first 64 KB]`, but takes no offset, so the remainder of a large note or conversation YAML is simply unreachable — the model either answers from the head or re-reads the same head. `web_fetch` already solves this with `start_index`/`max_chars` plus a continuation marker, and `list_directory`/`search_directory` already page with limit/offset; make `read_file` consistent with them. The per-turn budget now suppresses an exact repeat, so a model that reacts to truncation by re-reading gets a duplicate reference instead of the missing tail, which makes the gap more visible rather than less. Keep the default first page identical to today's behavior so existing prompts are unaffected, page by character offset (not bytes) so multi-byte content can't split, and state the remaining length in the marker. Cover truncation, continuation, an offset past the end, and multi-byte boundaries; extend the same treatment to the MCP schema so Claude/Codex see the new parameters.


<!-- Tier 2: failure salvage, data protection, and diagnosability. -->
- [ ] Make an expired Claude subscription login obvious and recoverable instead of a generic task failure. `claude-cli` turns fail with `Failed to authenticate: OAuth session expired and could not be refreshed`, surfaced as an ordinary turn error: a scheduled task records it as a run failure, emails the first one, and keeps failing on schedule until someone reads the message closely, while an interactive turn shows the raw CLI text with no indication that the fix is a login rather than a retry. It recurs because the subscription session expires on its own, and the durable fix (`claude setup-token`, stored as the provider's `oauthToken`) is interactive and cannot be performed by the failing process. Detect the authentication-expired shape from the `claude-cli` adapter specifically — not by matching "auth" anywhere in arbitrary provider output — and classify it as a distinct failure kind carried on the existing atomic failed-turn record. Surface it as an actionable state rather than a stack of identical errors: say which provider needs signing in again and what to run, link it from the task's failure row, and show it once in Settings next to that provider rather than once per failed run. A scheduled task that fails only for this reason should not keep emailing on every occurrence — reuse the existing first-failure-only behavior keyed on the classification. Do not attempt an automatic re-login, do not silently fall back to another provider or model, and never log or persist token values. Cover the classifier against real expired-session output and against unrelated CLI errors that must stay generic, the persisted failure kind, the Settings/task surfaces, and the email suppression.
- [ ] Preserve bounded provider-supplied reasoning when a conversation turn fails. `SessionInner.full_thinking` already retains up to 128 KiB for live replay, but `mark_error()` clears it and `AssistantWrite` has no reasoning field, so a long failed run can leave an empty assistant record even when the provider streamed useful working notes. On the existing atomic failed assistant record only, persist that buffer in a clearly named optional field, render it as collapsed **Incomplete model reasoning**, and include a labeled section in the generated Markdown preview for posterity. Never invent reasoning for providers that do not supply it, persist it for successful turns or metadata logs, or feed it back into later model context. Preserve the existing bound and truncation marker. Treat this as diagnostic salvage, not as a final answer or a substitute for preventing failure. Cover failures before and after visible content, reconnect/reload, Markdown generation, absent reasoning, truncation, and secret-canary exclusion from logs.
- [ ] Add native PDF attachments to conversations. Generalize the current image-only `Attachment`, pending-composer, queue, vault-storage, and server message types to support `document` attachments while preserving every existing image path. Accept `application/pdf` from the file picker and drag-and-drop, store the original PDF under `conversations/attachments`, and render an accessible filename chip before and after sending. Add a provider/model `supportsDocuments` capability and send PDFs only through verified native document protocols (initially Claude subscription and OpenRouter); unsupported providers such as Codex app-server and oMLX must block the attachment with a clear explanation rather than silently dropping it or uploading it elsewhere. Respect each upstream size/page limit, keep vault paths private, and never fall back from a local provider to cloud. Cover Tauri and browser/server modes with unit tests for capability gating and wire shapes plus a smoke test using a real small PDF.
- [ ] Implement manual conversation compaction end to end, then restore the **Compact now** action in the context-usage popover. Add an authenticated backend route that compacts the selected persisted conversation using the existing server-owned compaction/model-selection machinery, writes the compacted boundary atomically without dropping attachments, tool history, newer turns, or concurrent optimistic user messages, and returns the updated conversation. Reuse the automatic-compaction format and provider-native one-shot model behavior rather than creating a second summary schema. The client action must show progress, reload the resulting conversation, preserve scroll sensibly, and surface failures without leaving a false compacted state. Keep automatic compaction unchanged and cover Tauri and browser/server modes with Rust route/persistence tests and a frontend interaction test. Do not expose a placeholder button before the backend path works.

## Deferred (not scheduled — no `- [ ]`, so the runner skips it)

- **Show exact build identity if stale-build debugging becomes costly again.** Settings and startup logs already show the app version, while the shipped `SharedPortInUse` startup screen makes competing instances explicit and actionable. Development builds still share the same package version, so a future diagnostic could add the compile-time git commit and build timestamp to Settings and startup logs. Do this only if identifying a stale binary remains a recurring problem; the port-conflict portion is complete.
- **Optimize per-token Markdown rendering only after a measured regression.** `MarkdownContent` is currently re-parsed as streaming state changes, but the previously measured 284 ms typing stall came from the now-windowed 1,302-row sidebar, and current conversation responsiveness is acceptable. Do not optimize based on the old measurement. If streaming in a long Markdown/math/code conversation becomes observably slow, first profile render/parse counts, then isolate the active streaming row and memoize completed message bodies while preserving tool pills, wiki links, math, syntax highlighting, find-in-conversation, and auto-scroll. Do not virtualize the message list because browser find relies on its DOM.

## Future epics (not scheduled — no `- [ ]`, so the runner skips them)

- **Separate the vault into local and cloud zones.** Today the trust boundary is
  enforced when a file is read and forgotten when one is written: `private/`
  mounts are local-only, but anything a local model learns from them is persisted
  into `conversations/` and `notes/`, which every model can read — and `notes/` is
  writable by every model too. `memory.md` is worse: it is injected into every
  system prompt with no locality check, so anything in it reaches cloud providers
  on every turn. Per-feature taint tracking can close each hole individually, but
  the durable fix is structural: give the vault two zones with one-way flow —
  local models read both and write to the local zone, cloud models see only the
  cloud zone. The boundary becomes a location the user can see in Obsidian rather
  than a flag they cannot, and one rule covers conversations, derived notes,
  memory, riffs, and task output instead of each re-implementing it. Design
  through: which zone a conversation belongs to when its model is switched
  mid-thread (fixing the zone at creation and warning on switch looks better than
  moving files and breaking ids and links); wiki links that point from the cloud
  zone into the local one; splitting memory so a local memory is never sent to a
  cloud provider; and migrating existing conversations and notes by provenance
  rather than by hand.

- **Plugin architecture** — return Alloy to an extensible, plugin-oriented app.
  Core model: **Resource → Workspace → Host capabilities**. Dogfood the extension
  API with first-party Notes, Tasks, Comparison, Council, Riff, and eventually
  Chat modules; add an **MCP client** for external capabilities and declarative
  vault packs as the no-code on-ramp. Full design, boundaries, trust model, and
  sequencing in [docs/plugin-architecture.md](docs/plugin-architecture.md).
