<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <defs>
    <mask id="keyhole-mask" maskUnits="userSpaceOnUse">
      <rect x="-200" y="-200" width="400" height="400" fill="white"/>
      <circle cx="0" cy="0" r="14" fill="black"/>
      <rect x="-10" y="8" width="20" height="22" rx="5" fill="black"/>
    </mask>
    <!-- macOS-style drop shadow -->
    <filter id="shadow" x="-20%" y="-20%" width="140%" height="140%">
      <feDropShadow dx="0" dy="6" stdDeviation="14" flood-color="#000" flood-opacity="0.25"/>
    </filter>
  </defs>

  <!-- Background squircle with shadow - macOS specs: 412x412 with 50px margin, rx=93 -->
  <rect x="50" y="50" width="412" height="412" rx="93" fill="#FAF9F6" filter="url(#shadow)"/>

  <!-- Wheel centered -->
  <g transform="translate(256, 256) scale(0.84)">
    <!-- White interior fill behind the wheel -->
    <circle r="140" fill="#FFFFFF"/>

    <!-- GREEN spoke (underneath ring) -->
    <line x1="0" y1="-32" x2="0" y2="-152" stroke="#22C55E" stroke-width="26" stroke-linecap="butt"/>

    <!-- Outer ring -->
    <circle r="152" fill="none" stroke="#1E293B" stroke-width="28" stroke-linecap="butt" stroke-linejoin="miter"/>

    <!-- Dark spokes -->
    <line x1="0" y1="-32" x2="0" y2="-152" stroke="#1E293B" stroke-width="26" stroke-linecap="butt" transform="rotate(60)"/>
    <line x1="0" y1="-32" x2="0" y2="-152" stroke="#1E293B" stroke-width="26" stroke-linecap="butt" transform="rotate(120)"/>
    <line x1="0" y1="-64" x2="0" y2="-152" stroke="#1E293B" stroke-width="26" stroke-linecap="butt" transform="rotate(180)"/>
    <line x1="0" y1="-32" x2="0" y2="-152" stroke="#1E293B" stroke-width="26" stroke-linecap="butt" transform="rotate(240)"/>
    <line x1="0" y1="-32" x2="0" y2="-152" stroke="#1E293B" stroke-width="26" stroke-linecap="butt" transform="rotate(300)"/>

    <!-- Shield hub with keyhole mask -->
    <path d="M-48 -32 L-48 8 Q-48 56 0 72 Q48 56 48 8 L48 -32 Z" fill="#1E293B" stroke="none" mask="url(#keyhole-mask)"/>

    <!-- Dark handles - start closer to center, extend further out -->
    <rect x="-20" y="-215" width="40" height="90" rx="20" ry="20" fill="#1E293B" transform="rotate(60)"/>
    <rect x="-20" y="-215" width="40" height="90" rx="20" ry="20" fill="#1E293B" transform="rotate(120)"/>
    <rect x="-20" y="-215" width="40" height="90" rx="20" ry="20" fill="#1E293B" transform="rotate(180)"/>
    <rect x="-20" y="-215" width="40" height="90" rx="20" ry="20" fill="#1E293B" transform="rotate(240)"/>
    <rect x="-20" y="-215" width="40" height="90" rx="20" ry="20" fill="#1E293B" transform="rotate(300)"/>

    <!-- GREEN handle on top - start closer to center, extend further out -->
    <rect x="-20" y="-215" width="40" height="90" rx="20" ry="20" fill="#22C55E"/>
  </g>
</svg>
